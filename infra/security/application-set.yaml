apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: security
  namespace: argocd
  labels:
    com.sudoceo: security
spec:
  generators:
    - git:
        repoURL: https://github.com/sud0ce0/homelab-apps.git
        revision: HEAD
        directories:
          - path: infra/security/*
  template:
    metadata:
      name: "{{ path.basename }}"
      labels:
        com.sudoceo: security
    spec:
      project: security
      source:
        plugin:
          name: kustomize-build-with-helm
        repoURL: https://github.com/sud0ce0/homelab-apps.git
        targetRevision: HEAD
        path: "{{ path }}"
      destination:
        name: in-cluster
        namespace: argocd
      syncPolicy:
        automated:
          selfHeal: true
          prune: true
