crds:
  install: true
  keep: false

rbacProxy:
  securityContext:
    seccompProfile:
      type: RuntimeDefault


manager:
  securityContext:
    seccompProfile:
      type: RuntimeDefault
  resources:
    requests:
      cpu: 250m
      memory: 256Mi
    limits:
      cpu: 1000m
      memory: 1024Mi

serviceMonitor:
  enabled: true
  interval: 15s

grafanaDashboard:
  enabled: true
  folder: "dragonfly"