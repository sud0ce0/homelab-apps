apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: crds
  namespace: argocd
spec:
  sourceRepos:
    - "https://github.com/sud0ce0/homelab-apps.git"
  destinations:
    - namespace: "*"
      server: "*"
  clusterResourceWhitelist:
    - group: "*"
      kind: "*"
